services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fidari-harper-pilot
    ports:
      - "9926:9926"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    restart: unless-stopped
